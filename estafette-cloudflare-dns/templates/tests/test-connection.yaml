apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "estafette-cloudflare-dns.fullname" . }}-test-connection"
  labels:
{{ include "estafette-cloudflare-dns.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "estafette-cloudflare-dns.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
